# 스마트시티 SDSS 원천 데이터 파일 ➔ DB 아키텍처 개편 매뉴얼 (Keep vs Drop)

본 매뉴얼은 `빅프로젝트 관련자료\최종1차\데이터` 에 존재하는 원천 파일들이 데이터베이스(PostgreSQL/PostGIS)와 어떻게 맞물리는지 검증하고, 다목적 입지 선정 및 AI 감리 플랫폼의 경량화를 위해 **DB에 영구 적재 및 인덱싱해야 하는 필수 테이블(Keep)**과 **임시 파일 업로드/AI 감리 시점에 런타임 캐싱 처리하여 DB에서 과감히 제거할 테이블(Drop)**을 분리 규명합니다.

---

## 1. 🟢 [KEEP] DB 영구 적재 및 지리 인덱스(GIST) 필수 테이블
아래 데이터들은 (1) 용량이 수십~수백 MB로 거대하여 매번 업로드 시 타임아웃을 유발하거나, (2) 입지 추천 API 가동 시 PostGIS `ST_Distance` 등 고속 인덱싱 공간 연산이 필수로 수반되는 도시 기반 마스터 데이터군입니다.

| 로컬 원천 파일명 (Source File) | 매핑 테이블 (Target Table) | 적재 상태 (Rows) | DB 유지 사유 및 용도 (Keep Reason) |
| :--- | :--- | :--- | :--- |
| `sig.shp`, `sig.dbf` | `districts` | **유지** (1 row) | 자치구 행정구코드 마스터 데이터 |
| `emd.shp`, `emd.dbf` | `dong_boundaries` | **유지** (36 rows) | 행정동 경계 폴리곤 (동별 통계 조인 및 지도 렌더링용) |
| `LSMD_CONT_LDREG_11170_202606.dbf` | `cadastral_lands` | **유지** (6,524 rows) | 연속지적도 필지 MultiPolygon (지목, 국공유지 `ownership_type` 내포) |
| `서울시 금연구역 정보.csv`<br>`용산구_흡연구역_20240719.csv`<br>`용산구청_금연구역_목록.csv` | `restricted_zones` | **유지** (649 rows) | **금연구역(배제용)**과 **흡연구역(기설 밀집도 감점용)**의 2중 지리 연산 및 이격거리 채점용. |
| `서울시 버스정류소 위치정보.csv`<br>`서울시 역사마스터 정보.csv` | `transit_stations` | **유지** (414 rows) | 버스/지하철 지리적 포인트 마스터 (매번 업로드 시 90만 행 처리 타임아웃 유발로 고속 공간 인덱스 검색 필수) |
| `BUS_STATION_BOARDING...csv`<br>`CARD_SUBWAY_MONTH...csv` | `transit_passengers` | **유지** (314 rows) | 대중교통 이용객 월간 승하객 통계 (용산구 역 ID 기반 정적 조인용) |
| `LOCAL_PEOPLE_DONG_202605_YONGSAN.csv` | `population_stats` | **유지** (38 rows) | 행정동별 평균 생활인구 통계 (100MB가 넘는 거대 CSV로 매번 실무자가 업로드 시 서버 폭발 우려로 고정 시딩 필수) |

---

## 2. 🔴 [DROP] DB 스키마 제거 및 런타임 임시 업로드 전향 테이블
아래 테이블들은 적재량이 `0`이거나, 굳이 DB 공간 인덱싱을 타지 않고 실무관이 AI 감리(Step 1) 시 **런타임 임시 파일 업로드 및 JSON/메모리 캐싱**으로 처리하는 것이 훨씬 정합성 높은 경량화 대상들입니다. **조장 컨펌 하에 DB 스키마에서 전격 Drop(삭제)합니다.**

| 로컬 원천 파일명 (Source File) | 매핑 테이블 (Target Table) | 적재 상태 | 제거 사유 및 런타임 대체안 (Drop & FE Upload) |
| :--- | :--- | :--- | :--- |
| `서울시 어린이집 정보(표준 데이터).csv` | `childcare_centers` | **0 rows** | **제거:** 용도제한 보호구역(`restricted_zones`) 내 `childcare_center` 타입으로 어린이집 지리가 완벽하게 흡수 통합되어 있어 불필요. |
| `소상공인시장진흥공단_상가정보.csv` | `commercial_shops` | **0 rows** | **제거:** 용산구 점포 데이터만 매번 2.9억 바이트의 파일에서 필터링하는 파싱 부하가 막심하므로 제거. 상권 분포 점수는 AI 감리 시점에 업로드된 JSON 데이터셋으로 런타임 분석 대체. |
| `서울특별시 용산구_가로휴지통.csv` | `trash_bins` | **0 rows** | **제거:** 입지 추천 및 갈등 분석 지표에서 배제된 휴지통 테이블은 구조 정리를 위해 완전히 Drop. |
| `서울특별시 용산구_담배꽁초상습투기.csv` | `civil_complaints` (공간) | **38 rows** | **전향:** 행정동별 연간 민원 건수는 38개 행정동 매핑에 불과함. DB 공간 테이블이 아닌 AI 감리 시 실무자가 올리는 임시 민원 CSV 파싱을 통해 런타임 스코어링 처리. |
| *(로컬 매칭 파일 없음)* | `verified_precedents` | **0 rows** | **유지 (예외):** 운영 시 최종 준공되어 등록되는 행정 의사결정의 이력 스냅샷이므로 테이블은 유지하되 콜드 스타트 시에는 항상 비어있는 상태로 유지. |
