# 🚀 OmniSite SDSS 종합 초기 구동 가이드 (Initial Setup & Deployment SOP)

본 지침서는 **AWS 클라우드 인스턴스 신규 환경** 및 **로컬 개발자 PC 환경**에서 단 한 번의 오류 없이 **OmniSite SDSS v1.0.0-Final-Release** 시스템을 완전 구동(Zero-Error Setup)할 수 있도록 정리된 최신 통합 표준 초기 구동 가이드입니다.

---

## ☁️ PART 1. AWS 클라우드 인스턴스 (Lightsail / EC2) 5분 완공 가이드

AWS 우분투(Ubuntu) 인스턴스를 새로 생성한 후, 아래 **4단계 명령어**를 순서대로 복사하여 터미널에 실행하면 시스템 구축 및 데이터 시딩이 100% 자동 완료됩니다.

---

### 1단계: Swap 가상메모리 4GB + 도커(Docker) 자동 설치 (1분)
> Next.js Turbopack 프로덕션 빌드 중 메모리 부족(OOM) 멈춤 현상을 원천 방지하고 도커를 공식 설치합니다.

```bash
# 1. 4GB Swap 파일 생성 및 자동 구동 설정
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile swap swap defaults 0 0' | sudo tee -a /etc/fstab

# 2. 도커 공식 자동 설치 스크립트 실행
sudo apt update -y && sudo apt install -y git curl
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
newgrp docker
```

---

### 2단계: Git 클론 및 `.env` 서버 공인 IP 입력 (1분)
> **주의:** `NEXT_PUBLIC_API_URL`과 `CORS_ORIGINS`에 본인 인스턴스의 공인 IP를 정확히 적어주십시오.

```bash
# 1. 소스코드 클론 및 디렉터리 이동
git clone https://github.com/Jonghyeon-Bae/OmniSite.git omnisite
cd omnisite

# 2. .env 프로덕션 환경 변수 생성 (<YOUR_PUBLIC_IP>에 본인 서버 공인 IP 적용)
cat << 'EOF' > .env
POSTGRES_PASSWORD=admin1234_production_key
OPENAI_API_KEY=your_openai_api_key_here
NEXT_PUBLIC_API_URL=http://<YOUR_PUBLIC_IP>:8000
CORS_ORIGINS=http://localhost:3000,http://<YOUR_PUBLIC_IP>,http://<YOUR_PUBLIC_IP>:8000,*
EOF

# ※ IP 수정이 필요할 경우 nano 편집기 사용
nano .env
```

---

### 3단계: 프로덕션 도커 멀티 컨테이너 빌드 & DB 시딩 (2분)
> DB, 백엔드, 프론트엔드 3개 컨테이너를 가동하고 용산구 6,524개 지적 필지 마스터 데이터를 자동 주입합니다.

```bash
# 1. 프로덕션 도커 멀티 컨테이너 빌드 및 가동
docker compose -f docker-compose.production.yml up -d --build

# 2. 백엔드 내부에서 6,524개 필지 seed_db.py 콜드스타트 실행
docker compose -f docker-compose.production.yml exec backend python /workspace/seed_db.py
```

---

### 4단계: 가동 무결성 및 DB 건수 실측 검증
```bash
# 1. 3대 컨테이너 가동 상태 확인 (omnisite_prod_db, be, fe 모두 running/healthy 확인)
docker compose -f docker-compose.production.yml ps

# 2. PostgreSQL 6,524필지 / 6,509상가 / 268제한구역 적재 건수 검증
docker compose -f docker-compose.production.yml exec database psql -U Admin -d postgres -c "SELECT 'cadastral_lands' AS table_name, COUNT(*) FROM cadastral_lands UNION ALL SELECT 'commercial_shops', COUNT(*) FROM commercial_shops UNION ALL SELECT 'restricted_zones', COUNT(*) FROM restricted_zones;"
```

- **접속 주소**: `http://<YOUR_PUBLIC_IP>` (포트번호 없이 80포트로 웹화면 접속)
- **초기 관리자 계정**: ID `admin` / PW `admin1234`

---

## 💻 PART 2. 로컬 컴퓨터 (Local PC / Developer) 3분 원클릭 구동 가이드

개인 컴퓨터(Windows / Mac)에서 시연 또는 개발을 위해 단 3줄의 명령어만으로 시스템을 켜는 방법입니다.

### 1단계: 사전 준비물
1. **Docker Desktop** 프로그램이 설치 및 실행(녹색 불 Running) 상태여야 합니다.
2. 프로젝트 루트 폴더(`1.0-prototype`)의 `.env` 파일에 `OPENAI_API_KEY=sk-proj-...`를 입력하고 저장합니다.

---

### 2단계: 로컬 실행 3줄 명령어 (PowerShell / CMD)
```bash
# 1. 로컬 도커 멀티 컨테이너 빌드 및 실행
docker compose up -d --build

# 2. 용산구 6,524개 지적 필지 콜드스타트 시딩
docker compose exec backend python seed_db.py

# 3. 갈등 예측 머신러닝(XGBoost) 모델 적재
docker compose exec backend python app/scripts/train_css_model.py
```

- **접속 주소**: `http://localhost:3000`
- **관리자 계정**: ID `admin` / PW `admin1234`

---

## ❓ PART 3. 자주 묻는 질문 & 트러블슈팅 (Q&A)

### Q1. `docker compose ps`를 쳤는데 아무것도 안 나옵니다.
- **원인**: AWS 프로덕션 환경에서는 설정 파일명이 `docker-compose.production.yml`입니다.
- **해결**: `docker compose -f docker-compose.production.yml ps`와 같이 `-f` 옵션으로 파일명을 정확히 지정하여 조회해 주십시오.

### Q2. `logs -f backend` 실행 시 `Attaching to ...` 화면에서 멈춰있습니다.
- **원인**: 파이썬 Uvicorn의 표준 출력 버퍼링 현상으로, 신규 HTTP 요청이 발생할 때까지 대기하는 **정상 상태**입니다.
- **해결**: 다른 터미널에서 `curl http://localhost:8000/api/v1/system/health`를 날려 실시간 반응을 확인하시거나, 과거 로그 확인 시 `docker compose -f docker-compose.production.yml logs --tail 100 backend` 명령을 사용해 주십시오.

### Q3. 백엔드 또는 프론트엔드 빌드 중 서버가 멈추거나 튕깁니다.
- **원인**: 서버 물리 메모리(RAM) 부족으로 인한 OOM(Out of Memory) 프로세스 강제 종료입니다.
- **해결**: PART 1의 1단계 **`Swap 4GB 가상메모리 생성`** 스크립트를 먼저 실행한 후 빌드를 재시도해 주십시오.

---

**최종 수정일자**: 2026년 8월 13일  
**시스템 버전**: `v1.0.0-Final-Release`  
**개발책임 및 총괄**: Antigravity Senior Peer Development Team
